Does the Pano a component of `ReactJS`, `React Native` or `React VR` ? 
```
render() {
    return(
        <View>
           <Pano source={ {uri:'../static_assets/sample_pano.jpg'} }/>
        </View>
    );
}
```