Complete FetchData function in order to  show DevCCasa?

```jsx
const FetchData = ({}) => ()

const App = () => (
  <FetchData url={"#"}>
    {(name)=>(<h3>{name}</h3>)}
  </FetchData>
);

render(<App />, document.getElementById('root'));

```